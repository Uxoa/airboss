<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Crear una Reserva</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
<header>
    <a href="/" class="logo-link">
        <div class="logo-img">
            <img src="/images/logo01.png" alt="AirBoss Logo">
        </div>
        <span class="logo-text">AirBoss</span>
    </a>
    <nav>
        <a href="/dashboard">Dashboard</a>
        <a href="/perfil">My Profile</a>
        <a href="/reservas/view">My Bookings</a>
        <a href="/logout">Logout</a>
    </nav>
</header>

<main class="create-reservation-container">
    <h1>Reservar Vuelo</h1>

    <section class="flight-details">
        <h2>Detalles del Vuelo</h2>
        <p><strong>ID Vuelo:</strong> {{flight.flightId}}</p>
        <p><strong>Aerolínea:</strong> {{flight.airlineName}}</p>
        <p><strong>Origen:</strong> {{flight.origin}}</p>
        <p><strong>Destino:</strong> {{flight.destination}}</p>
        <p><strong>Fecha de Salida:</strong> {{flight.departureTime}}</p>
        <p><strong>Asientos Disponibles:</strong> {{flight.availableSeats}}</p>
    </section>

    <section class="reservation-form">
        <h2>Confirmar Reserva</h2>
        <form action="/reservas/crear" method="post">
            <!-- Información del usuario (autocompletada) -->
            <label for="userId">Usuario:</label>
            <input type="text" id="userId" name="userId" value="{{userId}}" readonly>

            <!-- Información del vuelo (autocompletada) -->
            <label for="flightId">ID Vuelo:</label>
            <input type="text" id="flightId" name="flightId" value="{{flight.flightId}}" readonly>

            <!-- Campo modificable: Número de Asientos -->
            <label for="numberOfSeats">Número de Asientos:</label>
            <input type="number" id="numberOfSeats" name="numberOfSeats" min="1" max="{{flight.availableSeats}}" required>

            <button type="submit" class="btn btn-primary">Reservar</button>
        </form>
    </section>
</main>

<footer>
    <p>&copy; 2025 AirBoss. All rights reserved.</p>
</footer>
</body>
</html>
